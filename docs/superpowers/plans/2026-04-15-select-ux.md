# Select UX — Composant partagé SelectField

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Centraliser les styles des menus déroulants dans un composant partagé `SelectField` en améliorant hover, focus clavier et séparateurs, sans modifier la structure ou le rendu global des formulaires.

**Architecture:** Nouveau composant `src/components/ui/SelectField.tsx` qui encapsule le pattern Headless UI Listbox complet avec les classes améliorées. Les deux formulaires remplacent leur bloc Listbox (~25 lignes) par `<SelectField …>` (5 lignes). Aucun autre fichier de style n'est créé.

**Tech Stack:** Next.js 16 / React 19 / TypeScript / Tailwind CSS v4 / Headless UI v2 / Lucide React

---

## Carte des fichiers

| Fichier | Action | Responsabilité |
|---|---|---|
| `src/components/ui/SelectField.tsx` | Créer | Composant Listbox partagé avec états améliorés |
| `src/components/contact/ContactForm.tsx` | Modifier | Remplace le bloc Listbox par `<SelectField>`, supprime imports inutilisés |
| `src/app/professionnels/demande-de-devis/page.tsx` | Modifier | Remplace le bloc Listbox par `<SelectField>`, supprime imports inutilisés |

---

## Task 1 — Créer `src/components/ui/SelectField.tsx`

**Files:**
- Create: `src/components/ui/SelectField.tsx`

- [ ] **Étape 1 : Vérifier que le dossier `src/components/ui/` existe ou le créer**

```bash
ls src/components/ui 2>/dev/null || mkdir -p src/components/ui
```

- [ ] **Étape 2 : Créer le fichier avec le contenu exact suivant**

`src/components/ui/SelectField.tsx` :

```tsx
"use client";

import { ChevronDown, Check } from "lucide-react";
import {
  Listbox,
  ListboxButton,
  ListboxOptions,
  ListboxOption,
} from "@headlessui/react";

const BUTTON_CLASS =
  "group w-full flex items-center justify-between rounded-xl border border-white/20 bg-white/10 px-4 py-3 text-white outline-none transition-colors hover:border-white/40 focus:border-rc-yellow focus-visible:ring-2 focus-visible:ring-rc-yellow/40 focus-visible:ring-offset-1 focus-visible:ring-offset-transparent data-[open]:border-rc-yellow";

const OPTIONS_CLASS =
  "absolute z-50 mt-1 w-full overflow-hidden rounded-xl border border-white/10 bg-[#1e2433] shadow-2xl shadow-black/60 outline-none";

const OPTION_CLASS =
  "group flex cursor-pointer items-center gap-3 px-4 py-3 text-sm text-white/80 transition-colors border-b border-white/[0.05] last:border-0 hover:bg-white/[0.12] hover:text-white data-[focus]:bg-rc-blue/20 data-[focus]:text-white data-[selected]:bg-rc-yellow/10 data-[selected]:text-rc-yellow";

const OPTION_EMPTY_CLASS =
  "group flex cursor-pointer items-center gap-3 px-4 py-3 text-sm text-white/40 transition-colors border-b border-white/[0.05] hover:bg-white/[0.12] hover:text-white/70 data-[focus]:bg-rc-blue/20 data-[focus]:text-white/70";

interface SelectFieldProps {
  value: string;
  onChange: (value: string) => void;
  options: readonly string[];
  placeholder?: string;
  allowEmpty?: boolean;
  emptyLabel?: string;
  "aria-labelledby"?: string;
}

export function SelectField({
  value,
  onChange,
  options,
  placeholder = "-- Sélectionnez --",
  allowEmpty = false,
  emptyLabel = "-- Optionnel --",
  "aria-labelledby": ariaLabelledby,
}: SelectFieldProps): React.ReactElement {
  return (
    <Listbox value={value} onChange={onChange}>
      <div className="relative">
        <ListboxButton
          aria-labelledby={ariaLabelledby}
          className={BUTTON_CLASS}
        >
          <span className={value ? "text-white" : "text-white/40"}>
            {value || (allowEmpty ? emptyLabel : placeholder)}
          </span>
          <ChevronDown
            className="h-4 w-4 text-white/60 transition-transform duration-200 group-data-[open]:rotate-180"
            aria-hidden="true"
          />
        </ListboxButton>

        <ListboxOptions className={OPTIONS_CLASS} transition>
          <div className="origin-top transition duration-150 ease-out data-[closed]:scale-y-95 data-[closed]:opacity-0 py-1">
            {allowEmpty && (
              <ListboxOption value="" className={OPTION_EMPTY_CLASS}>
                <span className="h-4 w-4 flex-shrink-0" />
                <span>{emptyLabel}</span>
              </ListboxOption>
            )}
            {options.map((option) => (
              <ListboxOption
                key={option}
                value={option}
                className={OPTION_CLASS}
              >
                <Check
                  className="h-4 w-4 flex-shrink-0 text-rc-yellow opacity-0 group-data-[selected]:opacity-100 transition-opacity"
                  aria-hidden="true"
                />
                <span>{option}</span>
              </ListboxOption>
            ))}
          </div>
        </ListboxOptions>
      </div>
    </Listbox>
  );
}
```

- [ ] **Étape 3 : Vérifier la compilation TypeScript**

```bash
npx tsc --noEmit
```

Résultat attendu : aucune erreur.

- [ ] **Étape 4 : Commit**

```bash
git add src/components/ui/SelectField.tsx
git commit -m "feat(ui): composant partagé SelectField avec états hover/focus améliorés"
```

---

## Task 2 — Mettre à jour `ContactForm.tsx`

**Files:**
- Modify: `src/components/contact/ContactForm.tsx:5-6` (imports)
- Modify: `src/components/contact/ContactForm.tsx:184-230` (bloc Listbox)

- [ ] **Étape 1 : Remplacer la ligne d'import lucide-react (ligne 5)**

Avant :
```tsx
import { Loader2, CheckCircle, XCircle, Send, ChevronDown, Check } from "lucide-react";
```

Après (supprime `ChevronDown` et `Check`, désormais internes à SelectField) :
```tsx
import { Loader2, CheckCircle, XCircle, Send } from "lucide-react";
```

- [ ] **Étape 2 : Supprimer la ligne d'import Headless UI (ligne 6)**

Supprimer entièrement :
```tsx
import { Listbox, ListboxButton, ListboxOptions, ListboxOption } from "@headlessui/react";
```

- [ ] **Étape 3 : Ajouter l'import SelectField après les imports existants**

Ajouter après les imports (après la ligne `import type { ContactFormData }...`) :
```tsx
import { SelectField } from "@/components/ui/SelectField";
```

- [ ] **Étape 4 : Remplacer le bloc Listbox (lignes 184–230) par l'appel SelectField**

Supprimer ce bloc :
```tsx
          <Listbox
            value={formData.type_evenement}
            onChange={(value) => updateField("type_evenement", value)}
          >
            <div className="relative">
              <ListboxButton
                aria-labelledby="type_evenement-label"
                className="group w-full flex items-center justify-between rounded-xl border border-white/20 bg-white/10 px-4 py-3 text-white outline-none transition-colors hover:border-white/40 focus:border-rc-yellow data-[open]:border-rc-yellow"
              >
                <span className={formData.type_evenement ? "text-white" : "text-white/40"}>
                  {formData.type_evenement || "-- Optionnel --"}
                </span>
                <ChevronDown
                  className="h-4 w-4 text-white/60 transition-transform duration-200 group-data-[open]:rotate-180"
                  aria-hidden="true"
                />
              </ListboxButton>

              <ListboxOptions
                className="absolute z-50 mt-1 w-full overflow-hidden rounded-xl border border-white/10 bg-[#1e2433] shadow-2xl shadow-black/60 outline-none"
                transition
              >
                <div className="origin-top transition duration-150 ease-out data-[closed]:scale-y-95 data-[closed]:opacity-0 py-1">
                  <ListboxOption
                    value=""
                    className="group flex cursor-pointer items-center gap-3 px-4 py-3 text-white/40 transition-colors hover:bg-white/10 hover:text-white/70"
                  >
                    <span className="h-4 w-4 flex-shrink-0" />
                    <span className="text-sm">-- Optionnel --</span>
                  </ListboxOption>
                  {EVENT_TYPES.map((type) => (
                    <ListboxOption
                      key={type}
                      value={type}
                      className="group flex cursor-pointer items-center gap-3 px-4 py-3 text-white/80 transition-colors hover:bg-white/10 hover:text-white data-[selected]:bg-rc-yellow/10 data-[selected]:text-rc-yellow"
                    >
                      <Check
                        className="h-4 w-4 flex-shrink-0 text-rc-yellow opacity-0 group-data-[selected]:opacity-100 transition-opacity"
                        aria-hidden="true"
                      />
                      <span className="text-sm">{type}</span>
                    </ListboxOption>
                  ))}
                </div>
              </ListboxOptions>
            </div>
          </Listbox>
```

Remplacer par :
```tsx
          <SelectField
            value={formData.type_evenement}
            onChange={(value) => updateField("type_evenement", value)}
            options={EVENT_TYPES}
            allowEmpty
            aria-labelledby="type_evenement-label"
          />
```

- [ ] **Étape 5 : Vérifier la compilation TypeScript**

```bash
npx tsc --noEmit
```

Résultat attendu : aucune erreur.

- [ ] **Étape 6 : Commit**

```bash
git add src/components/contact/ContactForm.tsx
git commit -m "refactor(contact): utilise SelectField partagé pour le champ type d'événement"
```

---

## Task 3 — Mettre à jour `demande-de-devis/page.tsx`

**Files:**
- Modify: `src/app/professionnels/demande-de-devis/page.tsx:5-6` (imports)
- Modify: `src/app/professionnels/demande-de-devis/page.tsx:215-251` (bloc Listbox)

- [ ] **Étape 1 : Supprimer la ligne d'import Headless UI (ligne 5)**

Supprimer entièrement :
```tsx
import { Listbox, ListboxButton, ListboxOptions, ListboxOption } from "@headlessui/react";
```

- [ ] **Étape 2 : Remplacer la ligne d'import lucide-react (ligne 6)**

Avant :
```tsx
import { ChevronDown, Check } from "lucide-react";
```

Après :
```tsx
import { SelectField } from "@/components/ui/SelectField";
```

- [ ] **Étape 3 : Remplacer le bloc Listbox (lignes 215–251) par l'appel SelectField**

Supprimer ce bloc :
```tsx
            <Listbox value={formData.typeEvenement} onChange={handleEventTypeChange}>
              <div className="relative">
                <ListboxButton
                  aria-labelledby="typeEvenement-label"
                  className="group w-full flex items-center justify-between rounded-xl border border-white/20 bg-white/10 px-4 py-3 text-white outline-none transition-colors hover:border-white/40 focus:border-rc-yellow data-[open]:border-rc-yellow"
                >
                  <span className={formData.typeEvenement ? "text-white" : "text-white/40"}>
                    {formData.typeEvenement || "-- Sélectionnez --"}
                  </span>
                  <ChevronDown
                    className="h-4 w-4 text-white/60 transition-transform duration-200 group-data-[open]:rotate-180"
                    aria-hidden="true"
                  />
                </ListboxButton>

                <ListboxOptions
                  className="absolute z-50 mt-1 w-full overflow-hidden rounded-xl border border-white/10 bg-[#1e2433] shadow-2xl shadow-black/60 outline-none"
                  transition
                >
                  <div className="origin-top transition duration-150 ease-out data-[closed]:scale-y-95 data-[closed]:opacity-0 py-1">
                    {EVENT_TYPES.map((type) => (
                      <ListboxOption
                        key={type}
                        value={type}
                        className="group flex cursor-pointer items-center gap-3 px-4 py-3 text-white/80 transition-colors hover:bg-white/10 hover:text-white data-[selected]:bg-rc-yellow/10 data-[selected]:text-rc-yellow"
                      >
                        <Check
                          className="h-4 w-4 flex-shrink-0 text-rc-yellow opacity-0 group-data-[selected]:opacity-100 transition-opacity"
                          aria-hidden="true"
                        />
                        <span className="text-sm">{type}</span>
                      </ListboxOption>
                    ))}
                  </div>
                </ListboxOptions>
              </div>
            </Listbox>
```

Remplacer par :
```tsx
            <SelectField
              value={formData.typeEvenement}
              onChange={handleEventTypeChange}
              options={EVENT_TYPES}
              aria-labelledby="typeEvenement-label"
            />
```

- [ ] **Étape 4 : Vérifier la compilation TypeScript**

```bash
npx tsc --noEmit
```

Résultat attendu : aucune erreur.

- [ ] **Étape 5 : Commit**

```bash
git add src/app/professionnels/demande-de-devis/page.tsx
git commit -m "refactor(devis): utilise SelectField partagé pour le champ type d'événement"
```

---

## Task 4 — Vérification visuelle et fonctionnelle

**Files:** aucun fichier modifié dans cette tâche

- [ ] **Étape 1 : Lancer le serveur de développement**

```bash
npm run dev
```

Ouvrir http://localhost:3000

- [ ] **Étape 2 : Vérifier le formulaire Contact (desktop)**

Naviguer vers la page Contact.
Vérifier :
- Le bouton du select affiche bien le texte placeholder grisé
- Au clic, le dropdown s'ouvre avec l'animation (scale + fade)
- Passer la souris sur chaque option → fond légèrement plus clair qu'avant (`bg-white/[0.12]`) + texte blanc
- Un séparateur très discret est visible entre les options
- Sélectionner une option → fond jaune/10, texte jaune, icône Check apparaît
- L'option vide "-- Optionnel --" est présente en tête de liste

- [ ] **Étape 3 : Vérifier le formulaire Devis (desktop)**

Naviguer vers `/professionnels/demande-de-devis`.
Vérifier les mêmes points que l'étape 2, sans l'option vide (ce champ est requis).

- [ ] **Étape 4 : Vérifier le focus clavier**

Sur chaque formulaire :
1. Tab jusqu'au bouton select → un ring jaune discret apparaît autour du bouton
2. Appuyer Entrée ou Espace → le dropdown s'ouvre
3. Utiliser les flèches Haut/Bas → chaque option active se distingue visuellement (fond bleu/20)
4. Appuyer Entrée → sélection confirmée, dropdown fermé
5. Appuyer Échap → dropdown fermé sans sélection

- [ ] **Étape 5 : Vérifier sur mobile (viewport 390px)**

Dans les DevTools du navigateur, passer en vue mobile 390px.
Vérifier :
- Le dropdown est positionné en dessous du bouton sans débordement horizontal
- Les options sont suffisamment hautes pour le tap (44px minimum — actuellement `py-3` = 12px top + 12px bottom + contenu ≈ OK)
- La sélection fonctionne au tap

- [ ] **Étape 6 : Vérifier l'absence de régression**

- Soumettre le formulaire Contact avec un type d'événement sélectionné → envoi OK
- Soumettre le formulaire Devis sans type d'événement → erreur de validation affichée (comportement existant conservé)
- Soumettre le formulaire Devis avec tous les champs remplis → envoi OK

- [ ] **Étape 7 : Commit final si ajustements mineurs**

Si des micro-ajustements ont été faits lors de la vérification :
```bash
git add -p
git commit -m "fix(select): ajustements visuels post-vérification"
```

Sinon, passer directement à l'étape 8.

- [ ] **Étape 8 : Push**

```bash
git push
```
